#!/usr/bin/env bash

USERNAME=$(whoami)
echo "Uninstalling Docker image and container for DTCC Builder for user "$USERNAME "..."
docker container rm dtcc-builder-$USERNAME -f
docker image rm dtcc-builder-image-$USERNAME -f
